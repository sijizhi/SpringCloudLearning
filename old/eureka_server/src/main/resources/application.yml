server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    #注册中心
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/