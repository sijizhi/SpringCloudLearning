<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--author： Zhi siJie -->



<mapper namespace="com.example.pagehelper.mapper.UserMapper">

    <select id="pageUsers" resultType="user">
      select * from idoc_user
    </select>

    <insert id="addUser" parameterType="user">
        INSERT INTO idoc_user(id,usercode,PASSWORD,username) VALUES(aid.nextval,#{userCode},#{passWord},#{userName})
    </insert>

</mapper>